<%= form_for [@coctail, @dose] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.label :ingredient %>
  <%#= f.association :ingredient %>
  <%= f.collection_select :ingredient_id, Ingredient.all, :id, :name %>
  <%= f.submit "Submit" %>

<% end %>
